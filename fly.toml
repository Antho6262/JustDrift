app = "discord-drift-bot"
primary_region = "cdg"

[build]

[env]
  NODE_ENV = "production"

[[services]]
  internal_port = 3000
  protocol = "tcp"

  [[services.tcp_checks]]
    interval = "15s"
    timeout = "2s"

[mounts]
  source = "bot_data"
  destination = "/app"
